spring.application.name=backend

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3307/reminiscence
spring.datasource.username=root
spring.datasource.password=iq2w3e4r!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:}
spring.mail.password=${EMAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=${EMAIL_FROM:noreply@youmemories.com}

# Application Configuration
app.frontend.url=${FRONTEND_URL:http://localhost:5173}
app.email-verification.expiration-hours=${EMAIL_VERIFICATION_EXPIRATION_HOURS:24}

# Server Configuration
server.port=8080